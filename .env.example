# Optional. Leave empty to use the built-in PGLite database.
DATABASE_URL=

# Better Auth — required for Sign in on a real deploy.
BETTER_AUTH_SECRET=
BETTER_AUTH_URL=http://localhost:8080

# Public host for OG images (no protocol).
VITE_PUBLIC_HOSTNAME=
